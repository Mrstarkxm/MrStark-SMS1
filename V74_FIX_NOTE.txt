V75 Persistence + Deployment Integrity Fix

Built from V74. Fixes the remaining concurrent-state race that could erase number allocations when a stale carrier-sync snapshot was merged after an allocation.

Changes:
- Stable collision-resistant IDs for users, numbers, ranges, CDRs, rate cards and request/history records.
- Supabase merge identity uses username/MSISDN/range externalId/carrier CDR id where available.
- Number allocation state has its own allocationUpdatedAt clock; stale snapshots cannot reset holder, owner, client, status or range.
- User updates carry updatedAt.
- /api/health build marker is V75 so the deployed version can be verified.

IMPORTANT: Keep the production Supabase state. Do not replace data/db.json with an old snapshot in Supabase.
